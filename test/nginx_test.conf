# Gate v0.92 vs Nginx 压力测试配置

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    
    server {
        listen 8081;
        server_name localhost;
        
        location / {
            return 200 "Nginx OK\n";
            add_header Content-Type text/plain;
        }
    }
}
